const cp = require("child_process"); const fs = require("fs"); const nodeExe = "C:\\project\\resources\\node\\node.exe"; const child = cp.spawn(nodeExe, ["-e", "console.log(JSON.stringify({NODE_OPTIONS:process.env.NODE_OPTIONS,NODE_PATH:process.env.NODE_PATH}))"], {stdio:["ignore","pipe","pipe"],env:{...process.env}}); let s=""; child.stdout.on("data",d=>s+=d); child.on("exit",()=>{console.log(s.trim());fs.writeFileSync("C:\\project\\scripts\\node-v24-env.json",s.trim());process.exit(0)});
